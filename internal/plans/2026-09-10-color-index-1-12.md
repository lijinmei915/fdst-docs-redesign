# FDS 有彩色索引 1-12 迁移 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use `fspec-dev-impelement-fe` or `executing-plans` to implement this plan task-by-task.

**Goal:** 将 FDS Base Palette 与固定 Dark Palette 的 12 阶 Token 索引从 `10-120` 一次性更名为 `1-12`，同步所有引用、构建产物、Skills 和文档。

**Architecture:** `tokens/fds-global.yml` 的 YAML import 图继续作为唯一事实源。人工只修改 Palette/Dark Token ID、Semantic/Effects 引用、生成器与契约测试；CSS、Catalog、Skill JSONL、Token 目录和静态文档站通过现有构建链重建。Gray 保持独立的 `10-200`、20 阶契约，不在本次范围内。

**Tech Stack:** YAML、Python 3、PyYAML、Python `unittest`、Bash Skill 查询脚本、Node.js Skill 合约测试。

---

### Task 1: 锁定新索引契约

**Files:**
- Modify: `tests/test_generate_color_palettes.py`
- Modify: `tests/test_build.py`
- Modify: `tests/test_export_catalog.py`
- Modify: `tests/test_query_tokens.py`

1. 将有彩色与 Dark Palette 的断言改为 `1-12`，并增加旧 `10-120` 有彩色 Token 不存在的断言。
2. 保留 Gray `10-200` 的断言，明确本次迁移边界。
3. 运行相关测试并确认在源文件修改前按预期失败。

### Task 2: 迁移 YAML 事实源与生成器

**Files:**
- Modify: `tokens/atomic/map/color/palette/*.yml`
- Modify: `tokens/atomic/map/color/dark/*.yml`
- Modify: `tokens/semantic/base/color.yml`
- Modify: `tokens/atomic/map/effects.yml`
- Modify: `tools/generate_color_palettes.py`

1. 按 `10->1`、`20->2`、...、`120->12` 更名 132 个 Base Palette Token 和 120 个 Dark Palette Token。
2. 同步 Semantic Color 与 Shadow 对有彩色 Token 的引用，保持解析值和状态映射不变。
3. 将生成器内部索引改为 `1-12`，保持第 9 阶 Seed 锚点和第 10 阶 Yellow Dark 特例。
4. 运行 Palette 对照、构建和相关单测。

### Task 3: 更新说明并重建所有派生产物

**Files:**
- Modify: `README.md`
- Modify: `docs/concepts/命名规则.md`
- Modify: `docs/engineering/YAML源文件.md`
- Modify: `docs/foundations/颜色.md`
- Modify: `docs/reference/迁移与常见问题.md`
- Generate: `dist/fds-global-tokens.css`
- Generate: `dist/fds-global-tokens.min.css`
- Generate: `dist/fds-token-catalog.json`
- Generate: `skills/fds-apply/references/fds-token-search.jsonl`
- Generate: `skills/fds-migrate/references/fds-token-catalog.jsonl`
- Generate: `docs/reference/Token目录.md`
- Generate: `public/`

1. 文档统一说明有彩色 `1-12`、Gray `10-200`，并更新示例变量。
2. 依次运行 `python tools/build.py`、`python tools/export_catalog.py`、`python tools/build_docs.py`。
3. 通过 Catalog 校验 Base 132 项与 Dark 120 项的 ID、数量、值和引用链。

### Task 4: 验证 Skill、文档和残留引用

**Files:**
- Verify: `skills/fds-apply`
- Verify: `skills/fds-migrate`
- Verify: `public/`

1. 运行全部 Python 测试、Skill 合约测试和 Skill 结构校验。
2. 运行 `build.py --check`、`export_catalog.py --check-docs`、`build_docs.py --check`。
3. 全仓扫描确认旧有彩色 `10-120` Token 已清零，Gray 与非颜色数值不受影响。
4. 检查 `git diff --check` 和最终差异，区分本次变更与既有未提交文档站改动。
