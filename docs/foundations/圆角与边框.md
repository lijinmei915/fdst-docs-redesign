---
layer: knowledge
type: spec
last_verified: 2026-09-08
teaches: "FDS 圆角、边框宽度和控件容器语义入口"
use_when: "选择控件、容器、卡片、胶囊的圆角或边框规格时"
---

# 圆角与边框

## 适用范围

本页说明 Atomic/Map 的圆角和边框宽度值域，以及控件和容器应使用的 Semantic 入口。

## 设计原则

- **圆角表达对象层级。** 控件、容器和胶囊形态按职责选择，不把圆角当作独立装饰。
- **同类对象保持一致。** 同一组件或同一层级容器不因页面不同随意换档。
- **语义入口优先。** 普通控件和容器使用 Base，场景卡片使用 Scene；Atomic 只作为底层实现。
- **边框同时考虑颜色。** Border Width 只决定几何宽度，状态和强调程度由语义颜色承担。

## 按场景选择

| 场景 | 推荐入口 | 说明 |
| --- | --- | --- |
| 普通输入、按钮等控件 | `--fds-g-control-radius` | 组件可在自身契约中映射 |
| 普通内容容器 | `--fds-g-container-radius` | 建立稳定容器层级 |
| 通用场景卡片 | `--fds-s-card-radius` | 与卡片边框、背景和阴影成组使用 |
| 胶囊、圆形底座 | `--fds-g-radius-full` | 仅在形态确实要求时使用 |
| 默认与强调边框 | `--fds-g-border-width-1`、`--fds-g-border-width-2` | 必须同时选择合适的语义颜色 |

## 视觉预览

[打开 Foundation 视觉预览](../examples/Foundation预览.html#radius)，对比常用圆角和边框宽度。

## 当前值域

| 类别 | 当前范围 | 适用属性 |
| --- | --- | --- |
| Radius | 0、2、4、6、8、12、16、9999px | `border-radius` |
| Border Width | 1、2px | `border-width` |

`9999px` 表示 full 圆角的固定枚举；它不是需要调用方计算的百分比。

## 使用规则

```css
.standard-control {
  border: var(--fds-g-border-width-1) solid var(--fds-g-border-default);
  border-radius: var(--fds-g-control-radius);
}

.content-container {
  border-radius: var(--fds-g-container-radius);
}
```

控件和普通容器优先使用 `control-radius`、`container-radius`。场景卡片优先使用 `--fds-s-card-border-*` 和 `--fds-s-card-radius`。

## Do / Don't

| Do | Don't |
| --- | --- |
| 按控件、容器、卡片或胶囊职责选择圆角 | 为了让页面“更柔和”统一放大所有圆角 |
| 同一类组件保持统一映射 | 在业务调用处逐个覆盖组件圆角 |
| 边框宽度与语义颜色共同评审 | 只加粗边框表达所有选中、焦点和错误状态 |
| 嵌套表面由组件维护几何关系 | 页面用 `calc()` 临时计算内外圆角 |

## 边界

- Border Width 只描述几何宽度，颜色和状态由 Semantic/Color 提供。
- `radius-full` 适合胶囊或圆形实现底座，不代表所有圆形组件都应直接消费 Atomic。
- 组件特殊轮廓和内部结构由组件 Token 负责。
- 本任务不调整现有圆角或边框数值。

## 来源

- [形状 Map](../../tokens/atomic/map/shape.yml)
- [布局语义](../../tokens/semantic/base/layout.yml)
- [场景语义源](../../tokens/semantic/scene/default.yml)
